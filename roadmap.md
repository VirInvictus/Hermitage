# Roadmap: Hermitage (Tokyo/3.14 Edition)

## Phase 1: The Engine (Python 3.14 Foundation)
- [x] **Repository Setup:** Modern Python 3.14 structure with `pyproject.toml`.
- [x] **Database Logic:** `database.py` with read-only Calibre `metadata.db` parser.
    - Implement the `Book` data model with PEP 649 annotations.
    - Joined query for books, authors, series, tags, ratings, comments, formats.
- [x] **FTS5 Search:** In-memory FTS5 index built from loaded books — prefix matching across titles, authors, tags, and series.
- [x] **Data Verification:** `hermitage-verify` CLI tool. Validates all book directories, cover files, and format presence. Bench-tests path resolution (69ms load + 59ms scan for 4,014 books).
- [x] **Asset Processing Worker:** Pillow median-cut color quantizer extracts 5 dominant colors per cover, sorted by vibrancy. Thread-pooled with disk + memory caching in `~/.cache/hermitage/colors/`.

## Phase 2: The Sanctuary (GNOME 50 Grid)
- [x] **Window Architecture:** Initialize `Adw.Application` with `AdwToolbarView` and `AdwHeaderBar`.
- [x] **The Gallery Grid:** Implement `Gtk.GridView` with `Gtk.SignalListItemFactory`.
    - Cover art cells with `Gtk.Picture` + `Gtk.Overlay` title labels.
    - CSS hover transforms (scale up) and fade-in title on hover/focus.
- [x] **Strict Aspect Ratios:** `Gtk.AspectFrame` with fixed 2:3 ratio and `overflow: hidden` clipping on the overlay. Cells don't stretch.
- [x] **Dynamic Styling:** Per-cell `CssProvider` maps dominant cover color to hover `box-shadow` glow and focus ring. Async color delivery for cold cache.
- [x] **Tokyo Breakpoints:** `Adw.Breakpoint` rules — narrow (<500sp): 2-3 cols, medium (<900sp): 3-5 cols, wide (default): 3-12 cols.

## Phase 3: The Codex (Premium Detail View)
- [x] **Layout:** Implement `Adw.OverlaySplitView` for the book sidebar.
- [x] **The Hero Banner:** Build a custom header widget that uses a heavily blurred, darkened version of the book cover as the background behind the title/author typography.
- [x] **Metadata Density:** Implement a wrapping flow box (`Gtk.FlowBox`) to render tags (e.g., Grimdark, Fantasy, Sci-Fi) as styled Libadwaita "pills" or chips.
    - Style the synopsis view using Libadwaita `.body` class and `Adw.Clamp` for high legibility.
- [x] **Native Handlers:** Connect a prominent, styled "Read" button to `Gtk.FileLauncher` to launch system PDF/EPUB readers (e.g., Foliate, Papers).

## Phase 4: Polish & Caching
- [x] **Thumbnailing:** Pillow-based thumbnail cache in `~/.cache/hermitage/thumbs/` with async generation and cache warming.
- [x] **Keyboard Navigation / Shortcuts:** Escape (dismiss codex/search/sidebar), Ctrl+F (toggle search), Ctrl+L (toggle virtual library sidebar).
- [x] **Search Parameters:** Calibre-compatible search bar with field prefixes (`tags:`, `title:`, `authors:`, `series:`, `formats:`, `rating:`), quoted values, exact match (`=`), boolean operators (`and`/`or`/`not`), parentheses, and `vl:` virtual library references. Slides below header via GNOME `Gtk.SearchBar` pattern.
- [x] **Virtual Library Support:** Left sidebar reading `virtual_libraries` from the Calibre `preferences` table. Clicking a library applies its search expression. Toggle via header button or Ctrl+L.
- [x] **Dismiss details button:** Close button overlaid on the Codex hero banner (top-right, circular, semi-transparent).

## Phase 5: Configuration & First Run
- [x] **Clean Ctrl+C exit:** `SIGINT` handler in `__main__.py` exits cleanly instead of spewing GTK error traces.
- [x] **Search debounce:** 400ms debounce on keystroke — grid doesn't re-filter until you stop typing. Clearing the search bar applies immediately.
- [x] **Clickable Codex metadata:** Clicking the author name, series, or any tag pill in the Codex populates the search bar with the corresponding filter (`authors:"Name"`, `series:"Name"`, `tags:"Tag"`).
- [x] **Tags as genre:** Tags are surfaced as the genre system throughout the UI.
- [x] **Genre browser:** Attractive category page (`GenreBrowser`) showing all tags organized by dot-separated hierarchy with book counts. Top-level categories as cards, sub-genres as clickable pills. Accessible via header bar toggle button.
- [x] **YAML config file:** `~/.config/hermitage/config.yaml` as the single source of truth. Stores library path, default sort order, grid density, and any future preferences. Human-readable and editable in any text editor.
- [x] **First-run wizard:** On launch with no config file, present an `Adw.Window` that walks the user through selecting a Calibre library directory (file chooser) and writes the initial config. No assumptions about where the library lives.
- [x] **Settings page:** In-app `Adw.PreferencesWindow` that reads and writes the same YAML config file. Sections for library path, sort order, and display preferences. Sort changes take effect immediately.
- [x] **Config migration:** `HERMITAGE_DB` environment variable still works as an override, but the wizard and settings page write to the YAML file. Precedence: env var > config file > wizard prompt.

## Phase 6: Sort & Browse
- [x] **Sort options:** Header bar sort menu with six fields (title, author, date added, publication date, rating, series) and ascending/descending toggle. Sort icon updates to reflect direction. Persisted to config. Also accessible from the preferences page.
- [x] **Series browsing:** When filtering by `series:`, the grid auto-sorts by `series_index` regardless of the global sort setting. Clearing the search restores the configured sort.
- [x] **Tag hierarchy browsing:** Genre browser exposes the full dot-separated tag tree (e.g., `Fic.Fantasy.Grimdark`) as nested cards with clickable pills. Clicking any genre filters the grid and switches back to the grid view.
- [x] **Author/series browse via Codex:** Clicking author or series in the detail view populates the search, effectively browsing by that dimension.

## Phase 7: Empty States & Error Resilience
- [x] **Placeholder covers:** Generate a styled placeholder for books without `cover.jpg` — display the title and author on a tinted card using the dominant color of the app accent or a neutral tone. No more invisible cells.
- [x] **No-results state:** When a search returns zero matches, show an `Adw.StatusPage` with a relevant message and suggestion (e.g., "No books match — try a broader search"). Replace the empty grid, don't just leave it blank.
- [x] **Loading progress:** Replace the static "Loading Library..." status page with an indication of progress — book count loaded, thumbnail cache warming percentage, or at minimum a spinner with a subtitle that updates.
- [x] **Graceful degradation:** Handle corrupt covers (truncated JPEG, zero-byte files) without crashing the thumbnail or color pipelines. Surface warnings in the UI or console log rather than silently swallowing them.
- [x] **Database lock handling:** If `metadata.db` is locked by Calibre, transparently fall back to a snapshot copy instead of failing or hanging. Mirror the approach in `../CalibreQuarry/src/cquarry/db.py` (`CalibreDB._open`): try `mode=ro` first; on `OperationalError` containing "locked", `shutil.copy2` the `.db` plus its `-wal` and `-shm` siblings to a `tempfile.mkstemp` path and open the copy. Log a one-line note that we're reading from a snapshot. Clean up the temp files on shutdown.

## Phase 8: Reading History
- [x] **Local history database:** A small SQLite database at `~/.local/share/hermitage/history.db` tracking which books have been opened via the "Read" button, with timestamps. Hermitage never writes to Calibre's database.
- [x] **Recently read shelf:** A "Recently Read" row or section at the top of the grid (or a virtual library entry) showing the last N books opened, sorted by last-opened time.
- [x] **Read indicator:** Subtle visual badge on cover cells for books that have been opened at least once — small dot, checkmark, or opacity shift. Unobtrusive but visible.
- [x] **Codex integration:** Show "Last read: 3 days ago" or similar in the Codex metadata section when a book has history.

## Phase 9: Polish & Visual Refinement
- [x] **Typography pass:** Bundled type system — Fraunces (display), Inter Variable (body), IBM Plex Sans Condensed (labels/pills). Fonts ship in `hermitage/fonts/` and register at startup via `Pango.FontMap.add_font_file()`; size, weight, tracking, and feature-settings tuned across every styled class. See v0.9.1 patchnotes.
- [x] **About dialog:** `Adw.AboutDialog` with version, description, license (GPL-3.0), author, and links (source repo, Ko-fi).
- [x] **Animations:** Smooth transitions for codex open/close, search bar slide, VL sidebar toggle. Review existing transitions for consistency and timing.
- [x] **Grid scroll position memory:** Remember scroll position when returning from a filtered view or after closing the codex. Don't jump back to the top.
- [x] **Tooltip refinements:** Hover tooltips on cover cells showing full title, author, and format list for long titles that get ellipsized.
- [x] **High-DPI audit:** *(v0.17.0, closed by the Phase 13 fractional-scale item below, which says so in its own text: thumbnails are now cached per scale tier under `thumbs/<scale>/` and sized from `get_scale_factor()` at bind, replacing the fixed 360x540 "2x" target. Ticked in the 2026-07-23 reconciliation sweep; the box had stayed open for two releases after the work landed.)* Verify thumbnail resolution, blur quality, and CSS shadows render sharply on 2x and 3x displays. Adjust `set_size_request` values if needed.
- [x] **Accessibility:** Ensure keyboard navigation works end-to-end (grid focus, codex navigation, search, VL sidebar). Screen reader labels on interactive elements.

## Phase 10: Packaging & 1.0 Release
- [x] **Application icon:** Design and ship a scalable SVG icon following the GNOME icon guidelines. Install to the hicolor icon theme. (`data/icons/hicolor/scalable/apps/io.github.virinvictus.hermitage.svg`, mirrored at project root as `logo.svg`.)
- [x] **Desktop entry:** `io.github.virinvictus.hermitage.desktop` file with proper categories, icon reference, and `StartupWMClass`. (Validated by `desktop-file-validate`.)
- [x] **AppStream metadata:** `io.github.virinvictus.hermitage.metainfo.xml` with release notes and OARS content rating for software center listings. Screenshots TODO once the icon and a release build are ready. (Validated by `appstreamcli`.)
- [x] **Flatpak manifest:** `io.github.virinvictus.hermitage.yml` targeting the GNOME 50 runtime. Bundle all Python dependencies. Test on a clean system. (Builds end-to-end with `flatpak-builder`; bundled `hermitage-verify` reads the host library through the sandbox in <200 ms.)
- [x] **Final testing:** Full regression pass on a 4,000+ book library. Verify every feature end-to-end: first-run wizard, search, VL sidebar, codex, read button, sort options, reading history, placeholder covers, breakpoints.
- [x] **Version 1.0.0:** Bump version, write release patchnotes, tag the release, publish Flatpak and PyPI package.

## Phase 11: CalibreQuarry Feature Port

Port as much of `../CalibreQuarry` (the `cquarry` CLI) into Hermitage as makes sense for a GUI library viewer. CalibreQuarry already has battle-tested logic for many things Hermitage will eventually want; rather than rewrite, lift the modules in `../CalibreQuarry/src/cquarry/` and adapt them to our `Book` dataclass and GTK surfaces.

- [x] **DB lock fallback** (see Phase 7 bullet) — port `CalibreDB._open` into `hermitage/database.py`. This is the highest-value port and a prerequisite for the rest, since cquarry runs against the same locked-DB conditions Hermitage will hit.
- [x] **Custom columns:** port `db.get_custom_columns()` and `db.load_custom_column()`. Surface user-defined Calibre custom columns in the Codex (read-only) and as searchable fields. (v0.18.0 — schema + values bulk-loaded in `database.load_library()` onto `Book.custom`, keyed by label; the Codex "Details" section renders text/enumeration columns as exact-match filter pills and datetime/number/bool as plain lines; searchable via `#label:` in `search.py`. Both storage shapes handled cquarry-style: normalized value+link tables and direct-storage columns like `date_read`.)
- [x] **Identifiers:** port `db.get_identifiers()` so the Codex can render ISBN / Goodreads / etc. links from the `identifiers` table.
- [x] **Library analytics & stats:** evaluate `cquarry/modes/{stats,analytics,audit}.py` for a "Library Insights" page (book counts by tag/author/series, missing-cover audit, format coverage).
- [x] **Catalog / export:** evaluate `cquarry/modes/{catalog,export}.py` for an in-app export (OPDS-style catalog, JSON/CSV dump). Hermitage stays read-only against Calibre's DB; exports go to the user's chosen path.
- [x] **Series view:** port `db.get_all_series()` (ordered series with index runs and titles) for a dedicated series browser sibling to the genre browser.
- [x] **Search-expression parity:** cross-check our `hermitage/search.py` parser against cquarry's `_parse_or` / `_match_tags` / `_match_authors` semantics (especially `tags:Foo` matching `Foo.*` as a hierarchy prefix, which Hermitage does not currently do). Align behaviour so the same query returns the same set in both tools.

## Phase 12: Test Suite & Lint Hygiene (workspace sweep, 2026-06-09)

The sweep found no behavioural bugs (everything compiles cleanly), but this is the weakest portfolio repo on hygiene: no tests and the most import lint in the portfolio set.

- [x] **Build a test suite.** Shipped in v0.16.0: 63 unittest tests under `tests/` in the CalibreQuarry style (stdlib unittest, temp sqlite fixtures). Covers the search grammar, the database layer against a hand-built `metadata.db` fixture (including the locked-DB snapshot fallback under a real `BEGIN EXCLUSIVE` lock), config, export, reading history, and the genre/series/insights aggregation builders. Run with `python -m unittest discover -s tests`.
- [x] **Shadowed module-level imports (4x F811):** the local re-imports in `app.py` are gone; the module-level `cfg_get` / `cfg_set` / `os` bindings are the one obvious binding. (v0.16.0)
- [x] **11x F401 unused imports** removed via `ruff check --fix`; the E741 ambiguous `l` in `insights.py` renamed. (v0.16.0)
- [x] **E402 noise control:** `[tool.ruff.lint.per-file-ignores]` in `pyproject.toml` ignores E402 under `hermitage/`; `ruff check hermitage/` is clean. (v0.16.0)
- [x] Commit the pending deletion of `claude.run` sitting in the working tree. (Folded into the v0.16.0 commit.)

## Phase 13: Hyprland-Leaning Design (Tiling WM Ergonomics)

Brandon's desktop moved from GNOME Shell to Hyprland, a Wayland tiling compositor. Nothing here regresses for a GNOME user; every item below is additive, aimed at making the app equally at home in a half, third, or quarter tile with no titlebar decorations drawn by the compositor. Grounded in a direct read of `hermitage/app.py`, `hermitage/codex.py`, and `hermitage/thumbnailer.py`; findings and file references are noted per item.

Direction note (Brandon, 2026-07-09): the portfolio goal has since moved past "runs politely under Hyprland" to "fully belongs on Hyprland," which means dropping Libadwaita while keeping GTK4; that lands as Phase 14, gated on the Colophon pilot. This phase's items stay valid (they are toolkit-agnostic geometry, keyboard, and scaling work), with two execution consequences: where an item offers a choice between adopting more Libadwaita (e.g. binding sidebar collapse to `Adw.Breakpoint`s) and a toolkit-agnostic fix (a manual width-notify threshold), prefer the toolkit-agnostic option so Phase 14 has less to unwind; and the "guard that theme reads go through `Adw.StyleManager`" item should guard "goes through the app's single theme-resolution path" instead, since Phase 14 replaces what that path is.

**Status (v0.17.0):** shipped together with Phase 14 as one migration. The additive, toolkit-agnostic items below (one-column floor, shortcuts window, genre/series accelerators, type-ahead find, fractional-scale thumbnails, the two guard tests, file-portal confirmation) are done; the sidebar-collapse item was superseded by the overlay redesign. **The manual visual pass ran 2026-07-28** and closed the last four items (stacked-minimum tile geometry, dialog sizing at a quarter tile, Home/End/PageUp/Down reaching the grid, CSD posture). Nothing needed lowering or rewiring: the window's true minimum is 346x263 in every sidebar state, every dialog shrinks well under a tile and cannot enlarge its parent, and the navigation keys reach `GtkListBase` untouched. **Phase 13's audit is complete**, with the optional chrome-hiding mode deliberately **deferred**. The pass turned up three incidental defects that are *not* geometry problems and are tracked as their own items below (secondary-window `app_id`, a stale module docstring, a missing `gi.require_version("Gdk", ...)`); Phase 13 closes once those are dispositioned.

- [x] **Sidebar collapse under narrow tiles:** *(v0.17.0 — superseded by the Phase 14 overlay redesign: both sidebars are now `Gtk.Revealer` panels floated over the grid in a `Gtk.Overlay`, so they never squeeze the grid on a narrow tile and no breakpoint-driven collapse is needed.)* ~~bind `collapsed` on both `codex_split` and `vl_split`~~ (`hermitage/app.py:930`, `hermitage/app.py:989`) to the same `Adw.Breakpoint`s that already drive grid column counts (`hermitage/app.py:1276-1297`). `Adw.OverlaySplitView` defaults to `collapsed=False`, so today an open Codex (`set_min_sidebar_width(360)`, `app.py:934`) or virtual-library sidebar (`set_min_sidebar_width(200)`, `app.py:993`) always pushes the grid rather than overlaying it. That's harmless on a full GNOME Shell workspace, but inside a half or quarter Hyprland tile it can squeeze the grid down to an unusable sliver. Collapsing should make both sidebars overlay once the window drops below the existing 900sp/500sp thresholds.
- [x] **A true one-column floor:** *(v0.17.0 — `grid.set_min_columns(1)`; GridView fits 1–12 columns by width, so a quarter-tile drops to a clean single strip.)* the narrowest existing breakpoint (`max-width: 500sp`, `app.py:1293`) still asks for `min-columns=2`. Add a further breakpoint (roughly 300 to 360sp, checked against `COVER_WIDTH = 180` at `app.py:45`) that drops to a single column, so a quarter-tile at common resolutions (for example a 480px-wide quarter of a 1920x1080 output) renders one clean strip of covers instead of two overcrowded or clipped ones.
- [x] **Stacked-minimum audit against real tile geometries:** *(2026-07-28 — measured, no change needed. The premise is architecturally moot: `main_overlay.set_measure_overlay(codex_revealer, False)` (`app.py:1125`) and the same call for `vl_revealer` (`app.py:1188`) exclude both sidebars from the window's measurement, so there is nothing to stack. GTK reports an identical **346x263** window minimum in all six states (nothing open / search / codex / VL / codex+VL / all three). The binding constraint is not a sidebar at all but the search bar at 346px, which comes from `_search_entry.set_size_request(300, -1)` (`app.py:791`); the grid measures 328, the header 267, and the Codex's 400px floor never reaches the window. 346px clears every realistic tile with room to spare: a third of 1920 is 640px, a sixth is 480px, a quarter of a 3440x1440 ultrawide is 860px. Verified visually at 640x1000 on the real session: three clean cover columns, no clipping, no refused resize. No minimum lowered.)* ~~sum the floors that can be simultaneously active~~: the grid at its narrowest, the Codex sidebar width (400px, `app.py:1116`), and the VL sidebar width (220px, `app.py:1179`). *(Numbers refreshed 2026-07-23: this item was written against the pre-v0.17.0 `Adw.OverlaySplitView` minimums at `app.py:934` / `app.py:993`, and against a 2-column grid floor of 2 times `COVER_WIDTH`. The overlay redesign floated both sidebars in a `Gtk.Overlay` and `set_min_columns(1)` dropped the grid floor to one column, so the sums are smaller than the item assumes. Re-measure before concluding anything.)* Test the window against common Hyprland tile sizes (halves and thirds of 1920x1080, quarters of a 3440x1440 ultrawide) with combinations of sidebars open, and confirm GTK can still shrink the window to fit rather than refusing the resize or clipping content. Lower a minimum if the sums don't fit realistic tiles.
- [x] **Dialog sizing at small tiles:** *(2026-07-28 — verified on the real session against a 640x1000 parent. The stated worry does not occur: every dialog is its own toplevel, so it floats over the parent and cannot enlarge it (the parent stayed exactly 640x1000 with Preferences and with Insights open). Measured minimums are far below any tile: Preferences 232x89 (default 480x420), Library Insights 434x89 (default 720x720), Keyboard Shortcuts 167x101 (default 440x520), About 420x401. One flag, not a failure: **Insights' 720x720 default is wider than a 640px third-tile and taller than a 580px quarter-tile**, so on a narrow tile it opens overhanging the parent on both sides. It renders correctly and is resizable down to 434 wide, so this is a default-size judgement call, not a layout bug. Left as-is pending a call on lowering the default.)* manually verify the dialogs stay usable and don't force the parent window wider than a quarter-tile when opened from a narrow Hyprland tile. *(Nouns updated 2026-07-23: this item was written against `Adw.PreferencesWindow` and `Adw.AboutDialog`, both deleted by the v0.17.0 de-adwaita migration along with the line numbers it cited. The successors are `PreferencesWindow(Gtk.Window)` in `hermitage/preferences.py` and the owned about dialog built in `app.py`'s `_on_about`. The task itself is unchanged and still unrun: it needs a manual pass on a real narrow tile.)*
- [x] **Shortcuts window:** *(v0.17.0 — an owned boxed-list dialog on `app.shortcuts` / Ctrl+? and in the primary menu, covering every binding and the Escape cascade.)* ~~there is currently no `Gtk.ShortcutsWindow`~~ (or equivalent) shipped anywhere in `hermitage/` or `data/`; the only way to discover a shortcut today is reading the `_setup_shortcuts` source (`app.py:733-793`). Add a shortcuts dialog covering Ctrl+F (search), Ctrl+L (VL sidebar), Ctrl+comma (preferences), Ctrl+I (insights), and the Escape cascade (codex, then search, then sidebar), so a keyboard-first Hyprland user without a habit of reaching for a menu bar can discover the app's bindings.
- [x] **Keyboard triggers for genre/series browsing:** *(v0.17.0 — `app.toggle-genres` on Ctrl+G, `app.toggle-series` on Ctrl+R.)* `_genre_btn` and `_series_btn` (wired around `app.py:909-925`) are mouse-only toggle buttons today; no accelerator reaches them. Add accelerators (registered via `set_accels_for_action`, alongside the existing `app.preferences` / `app.insights` bindings at `app.py:433-434`) so switching to the genre or series browser doesn't require a pointer.
- [x] **Type-ahead find in the Sanctuary grid:** *(v0.17.0 — `EventControllerKey` on the grid with a 1s buffer reset; the match logic is the pure `first_index_with_prefix`, unit-tested headlessly.)* `Gtk.GridView` has no built-in type-ahead the way `Gtk.ListView` or the tree widgets do, and none is wired up here. Add an `EventControllerKey` on the grid that accumulates typed characters (short debounce, mirroring the 400ms pattern already used in `_on_search_changed` for the search entry) and moves selection/focus to the first book whose sort title starts with the typed prefix. Extract the "find first index matching a prefix" logic as a pure function so it lands in `tests/` headlessly, matching the v0.16.0 test-suite style (stdlib unittest, no display required).
- [x] **Grid Home/End/Page navigation regression check:** *(2026-07-28 — confirmed clear on both interceptors. The `MANAGED` `Gtk.ShortcutController` (`app.py:853`) registers exactly three triggers, `<Control>f`, `<Control>l`, and `Escape`; a `Gtk.ShortcutController` only consumes a key when a registered trigger matches, so the navigation keys pass through untouched. The likelier culprit was the type-ahead `Gtk.EventControllerKey` on the grid (`app.py:1039`), which returns `True` (consuming the key) for anything with a printable Unicode point, but `Gdk.keyval_to_unicode` returns **0** for Home (0xFF50), End (0xFF57), Page_Up (0xFF55), Page_Down (0xFF56) and the arrows, so `_on_key` bails at the `unicode_point == 0` guard and the keys reach `GtkListBase`. Worth knowing for future edits: `Escape` maps to unicode 27, **not** 0, and is saved only by the `char.isprintable()` guard on the next line (`app.py:1021`) — weaken that guard and Escape stops reaching the shortcut controller.)* confirm the `Gtk.ShortcutController` scoped to `MANAGED` in `_setup_shortcuts` does not swallow Home/End/PageUp/PageDown before they reach `Gtk.GridView`'s own `GtkListBase` key bindings. These are inherited for free today; this is a manual GTK smoke-test item to make sure a future shortcut addition doesn't accidentally shadow them.
- [x] **Fractional-scale thumbnail audit:** *(v0.17.0 — thumbnails cached per scale tier under `thumbs/<scale>/`, sized from `get_scale_factor()` at bind; also closes the open Phase 9 High-DPI item.)* `THUMB_WIDTH`/`THUMB_HEIGHT` are a fixed 360x540 "2x" target (`hermitage/thumbnailer.py:37-38`) that is never checked against the window's actual scale factor. Hyprland commonly runs fractional scales (1.25x, 1.5x, and other non-integer values GNOME Shell rarely exposed), where a single fixed multiplier either wastes cache space or, past 2x, undershoots and softens covers. Read `Gtk.Widget.get_scale_factor()` at bind time and pick a thumbnail tier sized for the actual scale rather than one hardcoded constant, still caching per-tier under `~/.cache/hermitage/thumbs/`. This is the same class of question the still-open Phase 9 "High-DPI audit" item already flagged for GNOME's own HiDPI displays (roadmap line 71); resolve them together if convenient, but don't let this item block on that one.
- [x] **`app_id` / `StartupWMClass` regression guard:** *(v0.17.0 — `tests/test_guards.py::TestAppIdLockstep`.)* `APP_ID` (`hermitage/app.py:24`) already matches `StartupWMClass` in `data/io.github.virinvictus.hermitage.desktop`, which is exactly what Hyprland's `windowrulev2` needs to target the window by class. Add a test that reads both values and asserts they match, so a future rename can't silently break a user's Hyprland window rule without anyone noticing.
- [x] **No GNOME-only settings dependency, confirmed, now guard it:** *(v0.17.0 — dark/light goes through `hermitage/theme.py`'s portal read; `tests/test_guards.py::TestNoGSettings` fails on any `Gio.Settings` read.)* there is currently no `Gio.Settings` / `gsettings` read anywhere in `hermitage/`; dark/light already flows through the portal-backed `Adw.StyleManager` default rather than a GNOME-specific schema. Add a lint or test guard (a simple grep-based check is fine, matching the project's stdlib-lean style) that fails if a future patch introduces a raw `Gio.Settings` read for a GNOME-only schema (for example `org.gnome.desktop.interface`) instead of going through `Adw.StyleManager`.
- [x] **CSD posture with hidden window buttons:** *(v0.17.0 — the header is now a plain `Gtk.HeaderBar` with `show-title-buttons` off and Ctrl+Q to quit, so the hidden-buttons posture is the default. **Visual pass done 2026-07-28** at 640x1000 on the real Hyprland session: the header renders cleanly with no dead space where the window buttons would sit, no stranded controls, and no collision between the centred `WindowTitle` ("Hermitage" over a "7307 books" subtitle) and the button groups either side — four toggles left (VL, search, genres, series), the primary menu and sort button right. Rounded top corners and the Kanagawa surface read correctly against the compositor's own border. Nothing to change. The optional auto-hiding "chrome-hiding mode" stays **deferred** as out of scope.)* confirm the header renders cleanly with buttons hidden (Hyprland users commonly set `gtk-decoration-layout=":"` since the compositor draws no titlebar of its own). No code currently overrides `show-title-buttons`, so this should already work; verify it visually rather than assuming. Optionally, in keeping with the "visually immersive" mandate (`spec.md` section 3, hover-to-reveal grid titles), consider an opt-in chrome-hiding mode that auto-hides the headerbar on a maximized tile and reveals it on pointer proximity or Ctrl+F, the same reveal-on-intent philosophy the grid already applies to titles. Purely additive and off by default; drop it if it fights Codex/search discoverability more than it helps.
- [x] **Secondary windows report the wrong Wayland `app_id`** *(found 2026-07-28 during the visual pass; **fixed v0.18.1, 2026-08-09**. All four windows now pass `application=` at construction, matching what `SetupWizard` already did. The roadmap asked which of the two candidate fixes actually sets `app_id`; that was settled empirically rather than from the docs, with a four-window probe under one `Gtk.Application` on the live compositor: `application=` at construction and a later `app.add_window()` both yield the correct class, while a plain construction reports the prgname, reproducing the bug exactly. The construction kwarg won on being one line where the window is defined. The guard was extended as asked, but not by widening `TestAppIdLockstep` — that test compares `APP_ID` to `StartupWMClass` and structurally cannot see an unregistered window. New `TestSecondaryWindowAppId` walks the package AST instead and fails on any toplevel construction or toplevel-subclass `super().__init__` missing `application=`, reporting file and line; it carries a test that the detector itself fires, and was confirmed by reverting the fixes and watching it name all three sites.)* The main window correctly reports `io.github.virinvictus.hermitage`, but every secondary window reports **`python`**: verified live with `hyprctl clients` while Preferences was open (`class='python' title='Preferences'`), and the same applies to Library Insights, Keyboard Shortcuts, and About. Cause: these are plain `Gtk.Window`s (`preferences.py:38`, `insights.py:122`, and the two built inline in `app.py`), not `Gtk.ApplicationWindow`s, and they are never added to the `Gtk.Application`, so GTK falls back to `g_get_prgname()` for the surface's `app_id` — `python` under `python -m hermitage`, and `hermitage` under the console script, but never the app id in either case. This matters more here than in a GNOME app: it is exactly the class Hyprland window rules key on, so a user's rule targeting Hermitage silently misses all four dialogs, and it is the same lockstep that `tests/test_guards.py::TestAppIdLockstep` exists to protect (that test only covers `APP_ID` against the `.desktop` file, so it cannot see this). Fix is likely `application=self` on construction or `app.add_window(...)`; confirm which actually sets `app_id` before committing, and extend the guard test to cover secondary windows.
- [x] **Stale module docstring in `app.py`** *(found 2026-07-28; fixed v0.18.1, 2026-08-09; it now reads "Hermitage GTK 4 application (plain GTK; libadwaita was dropped in v0.17.0)").* `hermitage/app.py:1` still reads `"""Hermitage GTK 4 / Libadwaita application."""` even though Phase 14 removed libadwaita entirely at v0.17.0 and `tests/test_guards.py` now fails on any `Adw` import. One-line correction; flagged rather than fixed to keep the audit commit free of unrelated edits.
- [x] **Missing `gi.require_version("Gdk", "4.0")`** *(found 2026-07-28; fixed v0.18.1, 2026-08-09; the pin is added next to the `Gtk` one and a launch no longer prints the warning, confirmed by importing under `warnings.catch_warnings`).* Every launch prints `PyGIWarning: Gdk was imported without specifying a version first` from `hermitage/app.py:12`, because `Gdk` is imported alongside `Gtk` but only `Gtk` is version-pinned at `app.py:10`. Harmless today (GTK 4 pulls in Gdk 4), but it is noise on every start, and the project's own E402 per-file-ignore exists specifically to accommodate the `gi.require_version` pattern this line skips.
- [x] **File-portal usage, confirmed clean:** *(v0.17.0 — verified still true post-migration; all file access stays on `Gtk.FileDialog` / `Gtk.FileLauncher`.)* file access already goes through `Gtk.FileDialog` (`wizard.py:106`, `preferences.py:97`, `app.py:513`) and `Gtk.FileLauncher` (`codex.py:561`), both portal-native under Wayland; there is no legacy `Gtk.FileChooserDialog` / `Gtk.FileChooserNative` path to migrate. No action needed here beyond keeping any new file-picking code on these APIs.

## Phase 14: De-adwaita — Hyprland-Native Design (gated on the Colophon pilot)

Portfolio direction change (Brandon, 2026-07-09): the goal moved from "runs politely under Hyprland" (Phase 13's frame) to "fully belongs on Hyprland." Concretely: drop Libadwaita, keep GTK4 and PyGObject. GTK4 is Wayland-native and stays; what goes is the GNOME identity layer (the adwaita stylesheet, the adaptive widgets, `Adw.StyleManager`), replaced by plain GTK4 widgets and a stylesheet Hermitage owns outright, styled flat and tiling-first. Colophon pilots the move (its roadmap, Phase 6, the smallest shipped GTK app in the portfolio); Hermitage follows once the pilot's patterns are proven, translated from Rust to PyGObject. Hermitage was added to the portfolio sweep on request (2026-07-09, after the first five repos). It starts well positioned: `hermitage/style.css` already carries a substantial owned stylesheet (the `codex-*` and `cover-*` class vocabulary), file access is already fully portal-native (Phase 13's confirmed-clean item), and there is no GSettings dependency. "Never break userspace" binds in full: no feature regression, and the app keeps working under GNOME; the look stops being GNOME's, not the compatibility.

**Status (v0.17.0):** shipped 2026-07-11. Gate cleared (Colophon's pilot landed at v2.0.0, 2026-07-10). All 20 `Adw.` types replaced; the package imports no libadwaita (guarded by `tests/test_guards.py`). Automated acceptance passed (ruff, 75 unit tests, `hermitage-verify`, a clean launch); the manual visual pass on Hyprland is the remaining Phase 13 tail item.

- [x] **Go/no-go and inventory.** *(v0.17.0 — done; all 20 types mapped and migrated, see `hermitage/widgets.py` + the per-file changes.)* Review what Colophon's Phase 6 proved and cost, then map a plain-GTK replacement for each of the 20 `Adw.` types in use (inventoried 2026-07-09): `Application`/`ApplicationWindow`/`Window`, `HeaderBar`/`WindowTitle`/`ToolbarView`, the two `OverlaySplitView`s (`codex_split`, `vl_split`), `Breakpoint`/`BreakpointCondition`, `Clamp`, `StatusPage`, `Toast`/`ToastOverlay`, `AboutDialog`, and the preferences family (`PreferencesWindow`/`PreferencesPage`/`PreferencesGroup`, `ActionRow`/`SwitchRow`/`ComboRow`). Colophon's migration table is the template; the translation cost is PyGObject idiom, not design.
- [x] **Design decisions land in spec.md first.** *(v0.17.0 — spec.md §2a "Design Language" records all of it: hidden window buttons + Ctrl+Q, floating overlay sidebars, Kanagawa Dragon follow-system via the portal read, plain-`Gtk.Window` preferences.)* Decoration posture (headerbar cargo: search, the genre/series toggles, the primary menu, the window title; whether window buttons render at all, which dovetails with Phase 13's opt-in chrome-hiding idea and the spec's "visually immersive" mandate); the two sidebars' plain-GTK successors with an explicit narrow-tile stance replacing the breakpoint bindings; the preferences window's successor; and dark/light without `Adw.StyleManager` (a direct `org.freedesktop.portal.Settings` read via `Gio.DBusProxy`, no new dependency, or explicit-only theming; decide which).
- [x] **The owned stylesheet.** *(v0.17.0 — `style.css` now owns `.card`, `.boxed-list`, `.dim-label`, `.circular`, `.body`, `.error`, `.pill`, `.title-*`, `.suggested-action`, `.navigation-sidebar`, and the new widget classes, with the Kanagawa palette in `theme.py`. Deliberate deviation from "flat, square, hard borders": Hermitage keeps its rounded/immersive identity per the spec's "curated, not utilitarian" mandate.)* Grow `hermitage/style.css` from component classes into the full application sheet, replacing the adwaita stylesheet classes in use with owned equivalents.
- [x] **Packaging follow-through.** *(v0.17.0 — evaluated and stayed on the GNOME runtime, since that is where GTK4 and PyGObject come from; the manifest simply stops pulling libadwaita and CI drops `libadwaita-devel`. The app-id/`StartupWMClass` lockstep survives, now guarded by a test.)* The Flatpak runtime question is real here rather than automatic: the GNOME runtime is where GTK4 and PyGObject come from, so unlike the Rust apps Hermitage may stay on the GNOME runtime and simply stop importing `Adw`; evaluate honestly rather than assuming the freedesktop runtime is the answer. The app-id/`StartupWMClass` lockstep (Phase 13's regression-guard item) must survive untouched.
- [x] **Verification tail.** *(v0.17.0 — automated acceptance green: ruff, 75 unit tests including the new guards, `hermitage-verify` (7,116 books), and a clean launch. The theme-path guard now targets `hermitage/theme.py` (no `Gio.Settings`, no `Adw`). The keyboard, type-ahead, and fractional-scale items shipped; the geometry/CSD visual pass is the remaining manual Phase 13 item.)* Phase 13's items re-run against the migrated shell as this phase's acceptance criteria.

## Phase 4: cquarry Integration (Upcoming)

With the transition to the `cquarry` shared library (v1.2.0), Hermitage delegates all metadata parsing to the upstream engine. As `cquarry` executes on its roadmap, Hermitage will adopt the following features:

- **Saved Searches & Virtual Libraries:** Adopt `cquarry`'s upcoming JSON preference parser to render the user's Saved Searches in the sidebar exactly as configured in Calibre.
- **Hierarchical Tags & Categories:** Render a true GTK TreeView sidebar for hierarchical tags (`Fiction.SciFi`) using `cquarry`'s upcoming taxonomy parser.
- **JIT Single-Entity Fetching:** Transition to `get_book(book_id)` for detailed views, eliminating the memory overhead of loading full metadata for every book in the library at startup.
- **Reading Progress Tracking:** Map `last_read_positions` parsed by `cquarry` to render a visual progress bar indicating how far along the user is on their e-reader.
- **E-Reader Annotations:** Render e-reader highlights and annotations directly within Hermitage's Book Details pane, powered by `cquarry`'s FTS5 integration.
